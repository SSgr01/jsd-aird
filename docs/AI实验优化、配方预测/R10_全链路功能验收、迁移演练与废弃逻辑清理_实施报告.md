# R10 全链路功能验收、迁移演练与废弃逻辑清理实施报告

## 1. 结论

R10 当前为 **IN_PROGRESS**，尚不能标记 `COMPLETED`。V63 破坏性迁移、受保护数据保全、旧裸 `/api/v1/ai/*` 路由拒绝、V2 Python 服务、Java 编译与权限定向测试已通过；客户模板驱动的导入、实验本模板工作台、统一来源展示、实验完成后的事实投影和逐 Y 反馈回显，以及优化候选选择后继承客户模板创建 DRAFT 已完成浏览器核对。

仍有一项自动化收口阻断：

1. Java 全量 `mvn test` 共执行 439 个测试，报告 3 个失败、1 个错误、9 个跳过：既有 ArchUnit 模块边界违规，以及仓库缺失 `docs/word-import-smoke` 固件；AI R10 定向编译和权限测试仍通过，不能把全量结果写成通过。
旧 Java V1 运行类、V1 Java 契约和旧研究适配器已从源码删除；共享制品存储和 Parquet 编码能力保留在新 RND 训练链路使用的位置。Python 进程只注册 `/internal/v2`，旧 V1 路由返回404。

真实客户 SOP、值域、类别定义、误差门槛、材料身份和适用域尚未提供，R11 状态为 `WAITING_INPUT`。合成模型在隔离库中保持 `production_eligible=false`，不代表生产模型达标或已获生产启用。

## 2. 验收环境与边界

- API：`http://127.0.0.1:18080`，local profile，隔离数据库 `127.0.0.1:55434/r0506_browser`。
- Python：`http://127.0.0.1:18090`，只注册 `/internal/v2`，本地对象存储读取开关仅用于隔离验收。
- Web：`http://127.0.0.1:5175`。
- Worker：独立 worker profile，连接同一隔离数据库；旧 worker 已停止。
- 当前 `.env` 数据库和测试环境数据库均未执行 V63，也未执行任何破坏性操作。
- 客户模板为测试库中明确标识的副本：`应用测试报告模板`（版本 `4766ba63-17a1-4479-8d0e-d122c17805d8`）和 `综合测试报告模板`（版本 `4b75c667-2530-4bc5-a7cd-346959cd480b`）。

## 3. V63 迁移、保全与恢复

### 3.1 实际结果

在独立 PostgreSQL 18.4 临时集群中完成 V1～V62 后执行 V63。Flyway 记录 `63 | remove legacy ai runtime`；迁移后 `ai.training_dataset`、`ai.training_dataset_record`、旧 `ai.research_run`、旧 `ai.research_candidate`、旧 `ai.research_experiment_link`、旧 `ai.formula_model_*` 和 `ai.formulation_task_profile` 均不存在，`data.import_job.latest_training_dataset_id` 已删除，旧 `ai.model.manage` 权限已禁用，`ops.async_job` 增加拒绝旧 `RESEARCH_RUN`/`FORMULA_MODEL_BUILD` 类型的约束。V57 以后新表（含 `ai.research_run_v2`）及 `rnd.experiment_ai_source_evidence` 保留。

隔离库 V63 后保护数据数量为：公共文件 14、正式数据记录 9、实验 25、材料 2、公共审计 429、实验侧 AI 证据 20；业务库中 `production_eligible=true` 的模型数量为 0。新增 DRAFT 后数量随审计和实验证据递增，未覆盖或删除既有事实。当前预检返回 `COMPLETE_FOUNDATION`、Flyway 版本 63、旧表全部 `present=false`、`legacyLinks.total=0`、`readyForV63=false`（切换已完成，不允许再次执行）；半套领域临时库返回 `INCOMPATIBLE_FOUNDATION` 并列出缺失对象，未使用 `IF NOT EXISTS` 掩盖差异。

补充的 V62 事实库演练使用 R01 合成事实夹具：1 条正式数据记录、1 条实验版本和 1 条旧 AI 实验链接。证据保全脚本连续执行两次，随后 V63 完成后旧链接为 0、实验侧证据为 1、旧 AI 表全部为空，受保护数据摘要在迁移前后保持一致；结果归档于 `tmp/r10-evidence/v63-fact-preservation-rehearsal.json`。从 V63 隔离库生成完整逻辑备份并恢复到空库，恢复脚本返回 `manifestMatch=true`，结果归档于 `tmp/r10-evidence/backup-restore-verify.json`。最终结构断言（Flyway 63、旧表 0、旧权限禁用、旧任务类型约束存在、生产可用模型 0）归档于 `tmp/r10-evidence/v63-final-assertions.json`。

### 3.2 工具与可重复性

- 只读预检：`jsd-aird-api/src/main/resources/db/r10/preflight.sql`、`scripts/db-r10-preflight.ps1`、`scripts/db-r10-preflight.sh`。
- 破坏性入口要求显式 `-Apply`/`R10_APPLY=1`、目标数据库白名单和 `PGPASSWORD`；先确认精确 V62 基线，输出保护数据与删除清单，执行幂等证据保全，再复检 `readyForV63=true` 后才允许迁移。V63 后重复执行会因版本 63 和 `readyForV63=false` 停止。
- 备份和恢复工具复用 R01 的完整逻辑备份工具；备份写到仓库外临时目录，恢复后的受保护数据清单与原清单哈希一致。对象存储没有在 Flyway 中直接删除，需使用独立幂等清理工具。

### 3.3 尚缺证据

R10 迁移验收统一使用独立 PostgreSQL 18.4 临时集群。空库、事实库、不兼容库、V63 切换和备份恢复均已有真实数据库证据；Java 全量回归的 ArchUnit 模块边界和缺失 Word 固件失败已归档于 `tmp/r10-evidence/java-full-test.log`。

## 4. 浏览器全链路证据

### 4.1 来源与实验事实

1. 数据中心导入 `R07_应用测试_模拟实验_B.xlsx`（job `8a392795-cc24-4e97-9e86-b41e2f...`）和 `R07_综合测试_模拟实验_A.xlsx`（job `17f7de35-6180-4d10-8e35-2804f8...`）均显示真实 Univer 原文件、Sheet、字段和 3 条记录；字段映射确认后分别形成 `EXPERIMENT_DRAFT` 与 `DATA_ONLY` 结果。
2. 实验本模板入口复用同一客户模板和字段映射工作台；自由上传 job `eee31bb5-6df7-4cbe-86a8-c87acb98cf80` 保留原始文件并进入实验工作台，不显示字段映射。
3. 数据中心来源页显示数据中心上传、实验本来源和正式实验事实，实验来源跳转到实验详情；来源所有权没有被复制或转移。
4. 已完成实验 `EXP-20260917-84454D`（实验 `d8042be7-bad6-43c9-b780-e3b19d2cd6c8`）在优化页显示 60°光泽 52.3 GU 与 UV 表干“未表干”实测。反馈表显示两条 `COMPARABLE` 记录：光泽预测 50.8719、残差 1.4281；UV 预测“未表干”、命中为 true。

### 4.2 建模、训练和模型管理

- `/assistant/model-center?tab=settings`：模型中心的建模设置主页签显示 Y/X 两个顶层对象、目录状态、输入方案和材料配置；没有资格运行时显示真实待评估状态。
- `/assistant/model-center?tab=models`：模型中心的模型列表主页签显示自动学习开关、使用中/候选/数据积累汇总，以及按 Y 的模型状态。合成模型均标记非生产资格。
- Python `/internal/v2/health/live` 返回 200；`/internal/v1/health/live` 返回 404。Java 编译、权限目录测试通过。

### 4.3 正向性能预测

浏览器页面 `/assistant/performance-prediction` 已验证 4 个目标联合成功：60°光泽连续值、UV 表干二分类、外观等级多分类、铅笔硬度序数均返回类型化结果；逐 Y 门禁显示模型、输入、材料和适用域。此前 Python 文件 URL 未开启时四个目标均显示模型异常；重启 Python 并打开隔离验收文件 URL 后成功，故障结果作为系统失败证据保留。

### 4.4 配方预测

浏览器已验证多目标约束阻断（UV 最低概率过高）、无可行候选和刷新恢复；V2 运行状态与错误提示可见。本轮补跑光泽连续目标 `90eb0e84-7ecd-4bba-a177-4fa9c55d4fba` 返回 `SUCCEEDED`、4/4 候选，且冻结证据记录 `searchEngine=BAYBE`、`searchStrategy=CONTINUOUS_BAYBE`、`algorithm=BayBE ask-score-tell adaptive campaign`。随后在真实 Web 标签页 `http://127.0.0.1:5175/assistant/formula-prediction?runId=90eb0e84-7ecd-4bba-a177-4fa9c55d4fba` 刷新恢复同一运行，页面实际显示“候选对比”“4 / 4 个候选”和四张候选卡。候选总量精确为100%，模型、输入方案、QUALITY和适用域证据均随运行保存；原始响应归档于 `tmp/r10-evidence/formula-prediction-baybe-4-candidates.json`。

### 4.5 实验优化与客户模板草稿

在 `/assistant/experiment-optimization?runId=b28d8714-afd0-4858-abe6-e0aed1be4150` 完成：

1. 选择已完成实验基线 `EXP-20260917-84454D`；
2. 读取当前 60°光泽表现和已匹配的两条预测/实测反馈；
3. 运行后台优化并显示真实数量。当前运行返回 `PARTIAL`，保留 2 个不同策略候选并说明缺少策略，不凑数；
4. 选择候选后打开“创建实验草稿”；
5. 选择 `应用测试报告模板 · 第1版` 与 `R09实验优化验收`；
6. 创建成功，生成 DRAFT `0dea8331-bd5b-46f1-a453-f8c6b37af31b`，打开实验本后显示客户模板的 Univer 原生工作簿、Sheet1/Sheet2、保存草稿和发布入口。

该证据证明候选到客户模板 DRAFT 的链路；已完成实验的事实投影与反馈回显由上面的 `EXP-20260917-84454D` 证据覆盖。新建 DRAFT 尚未被误当作完成事实。

## 5. F01～F24 矩阵

| 编号 | 验收项 | 结果 | 证据/剩余工作 |
|---|---|---|---|
| F01 | 数据中心与实验本双来源独立供数 | PASS | 两入口 job、source owner 与来源页 |
| F02 | 实验接管、修订、作废不恢复旧导入 | PASS | R03/R09 实验版本与事实指针 |
| F03 | 同源去重、独立重复实验、验证分组 | PASS | R03 身份字段与资格证据 |
| F04 | 原始文件、坐标、映射和事实追溯 | PASS | Univer 原文件、SourceAnchor、反馈来源 |
| F05 | 事件重复/乱序与对账 | PARTIAL | R03/R09 运行证据有；需 R10 汇总日志包 |
| F06 | 98.8%和100%比例原值保留 | PASS | 合成数据、候选总量与实验模板 |
| F07 | 缺 X、未知材料、异常审查 | PASS | R04 三态和原因证据 |
| F08 | 新方案/字段覆盖率/实验后字段门禁 | PASS | 建模设置与资格接口；需汇总截图 |
| F09 | 人工决定历史失效 | PASS | R04 决定追加记录和重算隔离 |
| F10 | 业务幂等、同 Y 互斥、组织并发 | PASS | R05/R06/R09 worker evidence |
| F11 | Outbox、租约、取消、迟到 fencing | PASS | `tmp/r09-worker-evidence/`；需纳入统一归档 |
| F12 | 对象故障、制品故障和恢复 | PARTIAL | Python故障与 V2 失败已测；对象恢复 CI 待补 |
| F13 | 四类结果与文本/下界区分 | PASS | Python V2、浏览器四类结果 |
| F14 | 分组防泄漏与同折基线 | PASS | R05/R06 快照证据 |
| F15 | Candidate、启用、暂停、回退、单 ACTIVE | PARTIAL | 非生产合成模型正确拒绝生产启用；真实发布完整回退需 CI 汇总 |
| F16 | 性能预测逐 Y 门禁与部分成功 | PASS | `/assistant/performance-prediction` |
| F17 | 性能系统失败 503 与状态恢复 | PASS | 文件 URL 故障 503 及重启后恢复 |
| F18 | 配方目标/材料/X/总量约束 | PASS | 阻断与100/98.8证据；本轮 `90eb0e84-7ecd-4bba-a177-4fa9c55d4fba` 4候选运行 |
| F19 | 配方搜索策略和候选不足/无解 | PASS | 配方 `BAYBE/CONTINUOUS_BAYBE` 4候选及优化 `PARTIAL/BLOCKED` 证据 |
| F20 | 候选选择、DRAFT幂等、反馈可比性 | PASS | 客户模板 DRAFT + 已完成实验反馈 |
| F21 | 跨组织、运行所有者和来源权限 | PASS | 权限定向测试；浏览器越权需 CI 汇总 |
| F22 | V1～V63 空库/事实库/恢复 | PASS | 独立 PostgreSQL 18.4 临时集群完成 V1～V63、事实库切换和备份恢复，保护数据哈希一致 |
| F23 | 旧接口拒绝、旧任务不登记、旧表删除 | PASS | 裸旧 API 与 V1 Python 404、V63表删除、旧 Java V1 运行类/契约源码删除 |
| F24 | 页面刷新/关闭/重登录恢复与控制台 | PARTIAL | 多页刷新恢复 PASS；全量控制台/完整关闭重登记录待补 |

## 6. 自动化检查

- Java `mvnw -q -DskipTests compile`：PASS。
- Java `PermissionRouteFilterTest,IamPermissionCatalogTest`：PASS。
- Java `FlywayMigrationIT`：使用全新本地临时 PostgreSQL 数据库传入 `jsd.it.jdbcUrl` 执行 V1～V63，PASS；临时数据库迁移和恢复演练作为正式证据。最终日志归档于 `tmp/r10-evidence/flyway-local-test-v63-final.log`。
- Java 全量 `mvn test`：439 个测试中 3 个失败、1 个错误、9 个跳过；失败集中在既有 ArchUnit 模块边界和缺失 Word 固件，日志归档于 `tmp/r10-evidence/java-full-test.log`。
- Python V2 定向 `pytest tests/test_api.py tests/test_contract_v2.py tests/test_runtime_v2.py`：12 passed，1 个依赖弃用警告；JUnit 结果归档于 `tmp/r10-evidence/python-v2-tests.xml`。历史 V1 离线测试未计入 R10 通过数。
- Web `npm.cmd run typecheck`：PASS。
- Web `NODE_OPTIONS=--max-old-space-size=4096 npm.cmd run build`：PASS，包含 Vite 构建和 bundle gate。
- Web 全量 Vitest（配置 `testTimeout=60000`）：交付分支合并后复跑 54 个测试文件、208 个用例全部通过；研发测试列表和模板目录的重型用例在全量并发下通过。另有既有 React `act`/NaN 警告，尚未清理。
- R10 Shell 升级入口已与 PowerShell 对齐：先读取只读预检 JSON，只有 `readyForV63=true` 才执行保全和 Flyway；V63 后预检返回 `readyForV63=false` 并以退出码 2 阻断重复切换。`bash -n scripts/db-r10-upgrade.sh`：PASS。
- 旧裸接口：`/api/v1/ai/formulation-readiness`、`/api/v1/ai/research-requests/parse`、`/api/v1/ai/formula-predictions`、`/api/v1/ai/experiment-optimizations` 均 404；Python `/internal/v1/health/live` 404，V2 live 200。

## 7. 清理清单

### 已完成

- V63 删除旧 AI 表及导入任务旧训练集指针。
- 旧裸 `/api/v1/ai/*` 路由不再注册；Python 仅提供 V2。
- 旧训练数据集投影/查询新写入链路已移除；Worker 不接受旧 `RESEARCH_RUN`、`FORMULA_MODEL_BUILD` 任务。
- 数据中心提交只写入 `DATA_RECORDS_COMMITTED` Outbox；AI 由 `DataRecordsCommittedOutboxHandler` 异步投影统一事实，移除了 `ai ↔ data` 的直接循环依赖。数据导入定向测试通过，旧数据提交事务不再直接引用 AI 实现。
- Web 旧聊天研究页和旧客户端文件已删除；四个顶级入口及模型中心两个主页签走 `/api/v1/ai/rnd`。
- 旧 Java V1 service/repository/port、研究适配器和 V1 契约已删除；制品存储与 Parquet 编码保留在新 RND 训练链路。
- `ai.modeling_policy_version(kind=QUALITY)` 继续复用，没有新增 QUALITY 生命周期表。

### 待 R10 收口

- Python 运行服务已只注册 V2；仓库内供历史离线回放使用的 V1 库模块和工具仍未物理归档，已从在线入口断开，待 R10 清理窗口移出运行包后再删除，避免丢失历史验收复现能力。
- Java、Web 和 Python 在线源码静态扫描未发现旧 Controller、V1 契约、`CASE_STAT_RULE`/`HYBRID`、旧训练集指针或 `/internal/v1` 调用；扫描结果归档于 `tmp/r10-evidence/legacy-code-scan.txt`。
- 补齐对象存储恢复和权限越权测试的统一证据归档。
- 以独立幂等工具处理旧模型 bundle、Snapshot 和对象存储 key 的清理，不在 Flyway 中删除对象。

## 7.1 本轮补充：无配方模型与建模数据来源浏览器闭环

本轮在隔离验收库 `r10_usability_v64`（本地 PostgreSQL 18，端口 55434）补充了一条真实训练得到的“完全不使用配方”模型：

- 目标：`R10 无配方光泽`，类型为连续值；
- 冻结输入方案只包含实验前温度字段，没有配方组成字段；
- 当前 ACTIVE 模型为 `RANDOM_FOREST`，版本 6，`production_eligible=false`，仅用于隔离验收；
- 浏览器页面选择该目标后不显示配方编辑区，只显示温度输入。填写 22°C 后真实调用评分服务，返回 `全部完成`、`53.92 GU`，数据库预测记录为 `SUCCEEDED / SUCCEEDED / HTTP 200`（`305540cf-4f17-4d4c-a35d-e83e85e950ec`）。

建模数据来源闭环已用真实页面核对：

1. 目标详情“数据来源”显示数据中心找到 350 条、实验记录本找到 4 条、待确认 1 项；结果行展示来源样本和语义条件，并可执行“确认对应”。
2. 点击确认后返回 `数据来源已确认，历史样本正在重新评估`，历史映射版本保留，不覆盖旧版本。
3. 建模设置的“样本来源”列表同时显示数据中心与实验记录本的统一事实；进入“查看链路”可看到当前权威来源、来源版本、样本边界、物理来源组、内容哈希和可追溯状态。
4. 数据中心来源详情与实验记录本来源详情均可从页面进入，未发现跨模块复制或悬空来源。

上述浏览器验证运行在 5176 隔离前端、18080 API、18090 Python V2 和 18081 Worker；未触碰 5175 主服务、`.env` 数据库或测试环境数据库。

自动化收口结果：Python V2 定向测试 9 passed；Java IAM/Worker 定向测试通过；Web TypeScript、Vite 生产构建和 bundle gate 通过。本轮本机隔离验收按要求未执行 Docker/Testcontainers；包含 Docker 的 `FlywayMigrationIT` 未计入本轮通过项。生产和测试环境仍按 `compose.yaml` 使用 Docker 部署，容器配置已随功能分支交付。

## 7.2 本轮补充：模型中心与前端交付复核

在本地隔离服务保持运行期间再次打开真实页面复核：

- `/assistant/model-center?tab=models` 显示“模型列表/建模设置”统一入口、自动学习开关、模型目录/训练任务分段、算法列、使用中/候选/积累数据汇总；四类合成目标均有 ACTIVE（仅隔离验收，`production_eligible=false`），旧版本候选仍保留为历史记录。
- `/assistant/model-center?tab=settings` 显示 Y/X 两个建模对象及“新增预测目标”入口，没有把标准字段、来源映射或输入方案错误扩成第三个顶层导航。
- `/assistant/performance-prediction` 已实际返回连续、序数、二分类、多分类四类逐 Y 结果，并复核含无模型目标的部分成功状态。
- `/assistant/formula-prediction` 已实际显示连续目标 4/4 候选和无解运行的 0/4 阻断；运行刷新后仍能读取服务端冻结结果。
- `/assistant/experiment-optimization` 已实际显示已完成实验基线和客户模板工作台创建入口；既有证据覆盖四策略、客户模板 DRAFT、实验完成后的事实投影及逐 Y 反馈回显。

本轮前端检查使用 `npm.cmd`（避免 Windows 将 `npm` 当作应用打开）：首次默认堆大小构建 OOM，使用项目约定的 `NODE_OPTIONS=--max-old-space-size=4096` 后 `npm.cmd run build` 通过；`npm.cmd run typecheck` 通过。模板上传页同时修复了模板版本/实验分类经过前置校验后仍被 TypeScript 推断为可空的问题，热更新页面功能不变。

## 8. R10 完成前置项与 R11 输入

R10 完成前必须：处理 Java 全量回归中的 ArchUnit/Word 固件问题；归档 F01～F24 的截图、请求、响应和数据库断言。

R11 仍需客户提供真实来源文件、SOP、Y 类别和值域、材料编码、配方基准、独立批次/谱系、误差和概率门槛、适用域依据、比较集和数据保留策略。未齐备前不得把合成模型标为生产 ACTIVE。

## 9. 当前判定

**R10：IN_PROGRESS**；**R11：WAITING_INPUT**。本报告记录真实结果和阻断，不将计划中的 CI 或未完成页面测试写成通过。
